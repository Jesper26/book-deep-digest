---
name: book-deep-digest
description: 输入一本书名（或本地电子书文件），自动生成可反复翻阅的「全书精读回顾」材料：逐章提炼原文金句（附释义）+ 章节总结，再汇总出全书主线逻辑与思想框架，最后给出重点回顾提示与自测题，并可渲染成带搜索/折叠/答题的单文件 HTML 温习页。触发场景：全书总结、书籍精读、读书笔记、读后回顾、每章总结、章节金句、书籍思维导图、把一本书整理成复习材料、"帮我总结《XXX》"、"读完就忘怎么办"、book summary / chapter-by-chapter notes。
---

# 全书精读回顾生成器

把一本书压缩成一份**可反复温习**的精读材料：章节金句 + 章节总结 + 全书总结 + 回顾提示。
核心原则：**金句忠于原文，总结可被原文验证，宁可标注不确定也不编造。**

## 输入参数

| 参数 | 必填 | 说明 | 默认 |
| --- | --- | --- | --- |
| 书名 | 是 | 中文/外文书名，可带作者 | — |
| 作者 | 否 | 同名书歧义时消歧 | — |
| 文件路径 | 否 | PDF/EPUB/TXT/MD/DOCX 原文；有则质量最高 | 无 |
| 详略 | 否 | `完整`（默认）/ `精简`；见下方说明 | 完整 |
| 指定章节 | 否 | 如"只看第 3、5 章" | 全部 |
| 输出目录 | 否 | Markdown 与 HTML 落盘位置 | 当前工作目录 |

只有书名是必需的。**不要因为缺其他参数就停下来追问**——按默认执行，最后在第六节说明依据与置信度；只有"书名有歧义且无法判断"时才回问用户。

**详略档位**（默认 `完整`）：

| 档位 | 金句 | 章节总结 | 自测题 |
| --- | --- | --- | --- |
| 完整 | **不限条数，按质量筛** | 150–300 字 | 8–12 道 |
| 精简 | 每章 1–2 条 | ≤100 字 | 5 道 |

关键：**金句永远按质量筛选，不设数量上限，也不设下限**。某章确实没有够格的金句就写 1 条甚至不写，某章密度高就写 8 条——不要为了对齐而凑数，也不要因为怕长而砍掉真正的好句。

## 执行步骤

### 第 0 步：确认书与章节结构
1. 书名有歧义 → 列 2–3 个候选让用户确认。
2. 拿到**准确的目录/章节划分**再动笔，方法见 `references/source-strategy.md`。
3. 章节 > 20 时，鸟瞰表按"篇"归并行，但仍要覆盖全部章节。

### 第 1 步：拿原文（能拿就拿）
- 有本地文件 → 先跑切章脚本：
  ```bash
  python scripts/extract_book_text.py <file> --no-text          # 先看章节清单对不对
  python scripts/extract_book_text.py <file> --dump-dir ./chapters
  ```
  清单与真实目录不符时，调 `--min-chapter-words` 或给 `--toc-file`。
- 没有文件 → 联网检索目录与各章原文摘录；检索不到则退回模型知识，并按 `references/quality-rules.md` 降级处理金句。

### 第 2 步：写「书籍基本信息 + 全书结构鸟瞰」
按 `references/output-template.md` 第一节、第二节填写。长书（>15 章）在此处**暂停一次**让用户确认章节划分，再继续。

### 第 3 步：逐章精读（分批处理）
每批 3–5 章，产出：
- **金句：数量不限，只按质量标准筛**（有的章 1 条，有的章 8 条；宁缺毋滥，不许为凑数硬挑）。格式固定为 `>` 引用块 + `—— 位置：` + `**释义**` + `**为什么重要**`
- **章节总结**：核心主题 / 主要论点(≤3) / 关键脉络(A→B→C) / 关键概念 / 与全书的关联

筛选与措辞标准见 `references/quality-rules.md`。每批写完即追加进 Markdown 文件，不要攒到最后。

### 第 4 步：全书总结与回顾提示
- 全书总结**必须基于第 3 步已写好的各章总结**归纳，禁止凭印象重写。
- 必须包含：主线逻辑 / 核心观点关联表 / 整体思想框架 / 作者意图与背景 / 三个最该记住的结论。
- 回顾提示：高频回顾清单(5–8) / 自测题(8–12，答案单独成段) / 行动清单(3–5) / 易混淆点(3–5) / 复习节奏建议。

### 第 5 步：交付
1. 落盘 Markdown：`<书名>-精读回顾.md`（结构严格照模板，标题文字不改）。
2. 生成温习页：
   ```bash
   python scripts/build_review_page.py "<书名>-精读回顾.md"
   ```
   得到单文件 HTML 温习页：竖排目录 / 实时检索 / 章节折叠 / 金句分层排版（原句·译文·出处）/ 答案点击展开 / 纸墨双主题 / 打印存 PDF。
3. 用 `present_files` 同时呈现 HTML 与 Markdown。
4. 结尾用 3–5 句话复述"这本书最该记住的三件事"。

## 输出结构（固定六节，标题文字保持一致）

```
# 《书名》精读回顾
> 一句话总评
## 一、书籍基本信息        （表格 + 一句话定位 + 阅读价值）
## 二、全书结构鸟瞰        （章节 → 核心命题 表）
## 三、章节精读            （每章：一句话主旨 → 金句×N → 章节总结）
## 四、全书总结            （主线逻辑 → 观点关联表 → 思想框架 → 作者意图 → 三个结论）
## 五、重点回顾提示        （清单 → 自测题 → 行动清单 → 易错点 → 复习节奏）
## 六、内容来源与置信度    （来源档位 / 章节依据 / 金句置信度 / 存疑处）
```

完整字段与填写要求见 `references/output-template.md`；措辞红线见 `references/quality-rules.md`。

## 硬约束

1. **金句优先原文**：拿不到原文时只写高置信度名句，其余标注「[转述，非原文]」。绝不改写、拼接、编造。
1. **金句不限条数**：只按质量标准筛，不设上下限，禁止为凑数硬挑、也禁止为省篇幅砍掉好句。
2. **不编页码与章节**：无法定位就写能定位的层级（第 N 章 / 第 N 节），不写假页码。
3. **自测题答案单独成段**（前一行为空行），否则温习页"点击展开答案"失效。
4. **章节标题统一**为 `### 第 N 章 · 标题`，否则无法自动生成折叠卡片。
5. **存疑必写**：第六节"存疑处"不允许空着，没有就写"无"。

## 参考文件

- `references/output-template.md` —— 输出结构契约与填写要求，**生成前必读**。
- `references/quality-rules.md` —— 金句筛选、总结写法、字数密度、禁止事项。
- `references/source-strategy.md` —— 三档来源策略、长书分批、输入不足时的处理。
- `references/example-output.md` —— 完整示例（《孙子兵法》节选），不确定格式时对照它。
